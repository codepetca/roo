# Staging environment variables for Firebase Functions
# Copy this file and rename to .env when switching to staging

# Google API Keys (replace with staging-specific keys if needed)
# GEMINI_API_KEY=your-staging-gemini-key
# BREVO_API_KEY=your-staging-brevo-key

# Service account configuration for staging
# GOOGLE_APPLICATION_CREDENTIALS=path/to/staging-service-account.json

# Staging-specific configuration
ENVIRONMENT=staging
FIREBASE_PROJECT_ID=roo-staging-602dd

# Add other staging-specific environment variables here